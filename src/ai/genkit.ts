// Genkit integration removed.
// The project now uses Deepseek (when configured) or a local summarizer.
// This stub remains to avoid runtime import errors in older build artifacts.
export const ai: any = null;
